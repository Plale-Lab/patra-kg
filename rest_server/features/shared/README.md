# Shared Backend Helpers

Shared transport and provider helpers used by multiple PATRA features.

- `openai_compat.py`: OpenAI-compatible request helpers for LM Studio, LiteLLM, and similar providers
