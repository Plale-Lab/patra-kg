# Ask Patra

This feature provides the PATRA conversational assistant.

- Retrieval: searches latest approved model cards and datasheets
- Memory: stores conversation JSON files under the configured memory directory
- Prompts: seeds prompt templates into the configured prompts directory
- LLM: uses an OpenAI-compatible endpoint so local LM Studio and LiteLLM/SambaNova can share the same integration path

